<div id="controls">
    <%= link_to 'Cancel', @resource, :class => "cancel" %>
</div>

<h1>Editing contact for <%= @resource.display_name  %>  </h1>

<% form_for [@resource,@contact] do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :object => f, :locals => { :mode => "edit"} %>

  <%= f.submit 'Update Contact Information' %>
  <%= link_to 'Cancel', @resource, :method => 'get'%>

<% end %>

